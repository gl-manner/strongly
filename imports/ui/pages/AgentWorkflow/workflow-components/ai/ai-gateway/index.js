// imports/ui/pages/AgentWorkflow/workflow-components/ai/ai-gateway/index.js

// This file contains the static imports for the AI Gateway component
// It ensures Meteor includes these files in the bundle for dynamic import

// Whitelist for dynamic imports - this tells Meteor to include these files
if (false) {
  import('./metadata.js');
  import('./icon.jsx');
  import('./editor.jsx');
  import('./schema.js');
  import('./executor.js');
}
